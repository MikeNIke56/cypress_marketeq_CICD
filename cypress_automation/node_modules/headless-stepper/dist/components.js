"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/headless-stepper/src/lib/components/index.ts
var components_exports = {};
__export(components_exports, {
  Step: () => Step,
  Stepper: () => Stepper
});
module.exports = __toCommonJS(components_exports);

// packages/headless-stepper/src/lib/components/Stepper.tsx
var import_react4 = __toESM(require("react"));

// packages/headless-stepper/src/lib/context/index.tsx
var import_react = __toESM(require("react"));
var StepperContext = import_react.default.createContext(null);
StepperContext.displayName = "StepperContext";
var context_default = StepperContext;

// packages/headless-stepper/src/lib/hooks/useStepper.ts
var import_react3 = __toESM(require("react"));

// packages/headless-stepper/src/lib/hooks/useId.ts
var import_react2 = __toESM(require("react"));
var getRandomId = () => {
  if ("randomUUID" in crypto) {
    return crypto.randomUUID();
  }
  const uint32 = window.crypto.getRandomValues(new Uint32Array(1))[0];
  return uint32.toString(16);
};
var isReact18 = import_react2.default.version.includes("18");
var useIsomorphicId = () => {
  if (isReact18) {
    return import_react2.default.useId();
  }
  const localId = import_react2.default.useRef(getRandomId());
  return localId.current;
};

// packages/headless-stepper/src/lib/hooks/useStepper.ts
var useStepper = ({
  orientation = "horizontal",
  ...rest
}) => {
  const { steps, currentStep } = rest;
  const stepperId = useIsomorphicId();
  const progressId = useIsomorphicId();
  const labelId = `${useIsomorphicId()}-label`;
  const [_currentStep, setCurrentStep] = import_react3.default.useState(currentStep ?? 0);
  const hasPreviousStep = import_react3.default.useRef(false);
  const hasNextStep = import_react3.default.useRef(false);
  const stepElementsRef = import_react3.default.useRef([]);
  const currentStepFocused = import_react3.default.useRef(null);
  const totalSteps = import_react3.default.useRef(steps.length);
  hasPreviousStep.current = _currentStep < steps.length - 1;
  hasPreviousStep.current = _currentStep > 0;
  hasNextStep.current = _currentStep < steps.length - 1;
  const guessNextAvailableStep = import_react3.default.useCallback(
    (currentIndex) => {
      const nextStep2 = steps[currentIndex + 1];
      const isNextStepDisabled = nextStep2 == null ? void 0 : nextStep2.disabled;
      if (isNextStepDisabled) {
        return guessNextAvailableStep(currentIndex + 1);
      }
      return currentIndex + 1;
    },
    [steps]
  );
  const guessPreviousAvailableStep = import_react3.default.useCallback(
    (currentIndex) => {
      const previousStep = steps[currentIndex - 1];
      const isPreviousStepDisabled = previousStep == null ? void 0 : previousStep.disabled;
      if (isPreviousStepDisabled) {
        return guessPreviousAvailableStep(currentIndex - 1);
      }
      return currentIndex - 1;
    },
    [steps]
  );
  const nextStep = import_react3.default.useCallback(() => {
    if (_currentStep === (steps == null ? void 0 : steps.length) - 1)
      return;
    setCurrentStep((currentStep2) => {
      return guessNextAvailableStep(currentStep2);
    });
  }, [_currentStep, guessNextAvailableStep, steps == null ? void 0 : steps.length]);
  const prevStep = import_react3.default.useCallback(() => {
    if (_currentStep === 0)
      return;
    setCurrentStep((currentStep2) => {
      return guessPreviousAvailableStep(currentStep2);
    });
  }, [_currentStep, guessPreviousAvailableStep]);
  const _getNextArrayItem = (el) => {
    const currentIndex = stepElementsRef.current.indexOf(el);
    const nextItem = (currentIndex + 1) % stepElementsRef.current.length;
    return stepElementsRef.current[nextItem];
  };
  const _getPreviousArrayItem = (el) => {
    const currentIndex = stepElementsRef.current.indexOf(el);
    const previousItem = (currentIndex - 1 + stepElementsRef.current.length) % stepElementsRef.current.length;
    return stepElementsRef.current[previousItem];
  };
  const handleKeydown = import_react3.default.useCallback(
    (event, index) => {
      var _a, _b;
      const kbLeftRightArrowsKeys = [37, 39];
      const kbEnterSpaceKeys = [13, 32];
      const previousElement = stepElementsRef.current[index - 1];
      const nextElement = stepElementsRef.current[index + 1];
      const nextElToFocus = _getNextArrayItem(nextElement);
      const prevElToFocus = _getPreviousArrayItem(previousElement);
      const isNextElementDisabled = nextElement == null ? void 0 : nextElement.getAttribute("aria-disabled");
      const isNextElementDisabledBoolean = isNextElementDisabled === "true";
      const isPreviousElementDisabled = previousElement == null ? void 0 : previousElement.getAttribute("aria-disabled");
      const isPreviousElementDisabledBoolean = isPreviousElementDisabled === "true";
      if (kbLeftRightArrowsKeys.includes(event == null ? void 0 : event.keyCode)) {
        event == null ? void 0 : event.preventDefault();
        if ((event == null ? void 0 : event.keyCode) === 39) {
          if (isNextElementDisabledBoolean) {
            nextElToFocus == null ? void 0 : nextElToFocus.focus();
            currentStepFocused.current = stepElementsRef.current.findIndex(
              (el) => el.id === nextElToFocus.id
            );
            return;
          }
          (_a = stepElementsRef.current[index + 1]) == null ? void 0 : _a.focus();
          currentStepFocused.current = index + 1;
        }
        if ((event == null ? void 0 : event.keyCode) === 37) {
          if (isPreviousElementDisabledBoolean) {
            prevElToFocus == null ? void 0 : prevElToFocus.focus();
            currentStepFocused.current = stepElementsRef.current.findIndex(
              (el) => el.id === prevElToFocus.id
            );
            return;
          }
          (_b = stepElementsRef.current[index - 1]) == null ? void 0 : _b.focus();
          currentStepFocused.current = index - 1;
        }
      }
      if (kbEnterSpaceKeys.includes(event == null ? void 0 : event.keyCode)) {
        setCurrentStep(currentStepFocused.current);
      }
    },
    []
  );
  const stepperProps = import_react3.default.useMemo(
    () => ({
      role: "tablist",
      "aria-orientation": orientation
    }),
    [orientation]
  );
  const stepsProps = import_react3.default.useMemo(() => {
    return steps == null ? void 0 : steps.map((step, index) => {
      return {
        role: "tab",
        tabIndex: index === _currentStep ? 0 : -1,
        id: `${stepperId}-${index}`,
        "aria-posinset": index + 1,
        "aria-setsize": steps.length,
        "aria-disabled": step == null ? void 0 : step.disabled,
        "aria-current": index === _currentStep ? "step" : void 0,
        "aria-selected": index === _currentStep,
        onKeyDown: (event) => handleKeydown(event, index),
        onClick: () => step.disabled ? null : setCurrentStep(index),
        ref: (element) => stepElementsRef.current[index] = element
      };
    });
  }, [_currentStep, handleKeydown, stepperId, steps]);
  const progressProps = import_react3.default.useMemo(
    () => ({
      "aria-valuenow": _currentStep,
      "aria-valuemin": 0,
      "aria-valuemax": steps.length - 1,
      role: "progressbar",
      "aria-labelledby": labelId,
      tabIndex: -1
      // prevent focus
    }),
    [_currentStep, labelId, steps]
  );
  const state = import_react3.default.useMemo(
    () => ({
      labelId,
      progressId,
      currentStep: _currentStep,
      hasPreviousStep: hasPreviousStep.current,
      hasNextStep: hasNextStep.current,
      totalSteps: totalSteps.current
    }),
    [progressId, labelId, _currentStep]
  );
  return {
    stepperProps,
    stepsProps,
    progressProps,
    state,
    nextStep,
    prevStep,
    setStep: setCurrentStep
  };
};

// packages/headless-stepper/src/lib/components/Stepper.tsx
var import_jsx_runtime = require("react/jsx-runtime");
var Stepper = import_react4.default.forwardRef(
  ({
    currentStep = 0,
    orientation = "horizontal",
    children,
    className,
    as,
    ...rest
  }, ref) => {
    var _a, _b, _c;
    const AsComponent = as || "nav";
    const id = useIsomorphicId();
    const stepsAsChildren = import_react4.default.Children.toArray(
      children
    );
    const stepperHookValues = import_react4.default.useMemo(() => {
      return {
        currentStep,
        orientation,
        steps: stepsAsChildren.map((step) => step.props)
      };
    }, [currentStep, orientation, stepsAsChildren]);
    const stepperHook = useStepper(stepperHookValues);
    const contentProps = import_react4.default.useMemo(
      () => {
        var _a2, _b2, _c2, _d;
        return {
          role: "tabpanel",
          id: `step-content-${id}-${(_b2 = stepperHook.stepsProps[(_a2 = stepperHook == null ? void 0 : stepperHook.state) == null ? void 0 : _a2.currentStep]) == null ? void 0 : _b2.id}`,
          "aria-labelledby": `step-label-${id}-${(_d = stepperHook.stepsProps[(_c2 = stepperHook == null ? void 0 : stepperHook.state) == null ? void 0 : _c2.currentStep]) == null ? void 0 : _d.id}`,
          "aria-expanded": true
        };
      },
      [id, stepperHook.state.currentStep, stepperHook.stepsProps]
    );
    const items = stepsAsChildren.reduce(
      (acc, step, index) => {
        var _a2, _b2;
        const isDisabled = step.props.disabled;
        acc.push(
          import_react4.default.cloneElement(step, {
            disabled: isDisabled,
            "aria-controls": `step-content-${id}-${(_b2 = stepperHook == null ? void 0 : stepperHook.stepsProps[(_a2 = stepperHook == null ? void 0 : stepperHook.state) == null ? void 0 : _a2.currentStep]) == null ? void 0 : _b2.id}`,
            ...stepperHook == null ? void 0 : stepperHook.stepsProps[index]
          })
        );
        return acc;
      },
      []
    );
    const stepContent = (_c = (_b = stepsAsChildren[(_a = stepperHook == null ? void 0 : stepperHook.state) == null ? void 0 : _a.currentStep]) == null ? void 0 : _b.props) == null ? void 0 : _c.children;
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(context_default.Provider, { value: stepperHook, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AsComponent, { ref, id, ...stepperHook.stepperProps, ...rest, children: items }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { ...contentProps, children: stepContent })
    ] });
  }
);
Stepper.displayName = "Stepper.Root";

// packages/headless-stepper/src/lib/components/Step.tsx
var import_react5 = __toESM(require("react"));
var import_jsx_runtime2 = require("react/jsx-runtime");
var Step = import_react5.default.forwardRef(
  (props, ref) => {
    const { label, disabled, style, as, ...rest } = props;
    const AsComponent = as || "button";
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
      AsComponent,
      {
        ref,
        style: {
          pointerEvents: disabled ? "none" : "auto",
          opacity: disabled ? 0.5 : 1,
          ...style
        },
        ...rest,
        children: label
      }
    );
  }
);
Step.displayName = "Stepper.Step";
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  Step,
  Stepper
});
//# sourceMappingURL=components.js.map