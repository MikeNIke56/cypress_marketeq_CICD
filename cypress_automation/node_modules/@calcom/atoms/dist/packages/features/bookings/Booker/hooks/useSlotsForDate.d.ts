import { Slots } from '../../../calendars/lib/types';
type Slot = Slots[string][number] & {
    showConfirmButton?: boolean;
};
/**
 * Gets slots for a specific date from the schedule cache.
 * @param date Format YYYY-MM-DD
 * @param scheduleCache Instance of useScheduleWithCache
 */
export declare const useSlotsForDate: (date: string | null, slots?: Slots) => import('../../../calendars/lib/types').SlotInfo[];
export declare const useSlotsForAvailableDates: (dates: (string | null)[], slots?: Slots) => {
    readonly slotsPerDay: {
        date: string | null;
        slots: Slots[string];
    }[];
    readonly setSlotsPerDay: import('react').Dispatch<import('react').SetStateAction<{
        date: string | null;
        slots: Slots[string];
    }[]>>;
    readonly toggleConfirmButton: (selectedSlot: Slot) => void;
};
export {};
//# sourceMappingURL=useSlotsForDate.d.ts.map