import { BookingRepository } from '../../repositories/BookingRepository';
export interface IFilterHostsService {
    bookingRepo: BookingRepository;
}
export declare class FilterHostsService {
    readonly dependencies: IFilterHostsService;
    constructor(dependencies: IFilterHostsService);
    filterHostsBySameRoundRobinHost<T extends {
        isFixed: false;
        user: {
            id: number;
            email: string;
        };
    }>({ hosts, rescheduleUid, rescheduleWithSameRoundRobinHost, routedTeamMemberIds, }: {
        hosts: T[];
        rescheduleUid: string | null;
        rescheduleWithSameRoundRobinHost: boolean;
        routedTeamMemberIds: number[] | null;
    }): Promise<T[]>;
}
//# sourceMappingURL=filterHostsBySameRoundRobinHost.d.ts.map