import { ToggledConnectedCalendars } from '../types';
import { EventBusyDate } from '@calcom/types/Calendar';
type OverlayCalendarConnectedCalendar = {
    calendars?: {
        externalId: string;
        primary: boolean | null;
    }[];
    credentialId: number;
};
export declare const useOverlayCalendar: ({ connectedCalendars, overlayBusyDates, onToggleCalendar, }: {
    overlayBusyDates?: EventBusyDate[];
    onToggleCalendar: (calendarsToLoad: Set<ToggledConnectedCalendars>) => void;
    connectedCalendars: OverlayCalendarConnectedCalendar[];
}) => {
    isOpenOverlayContinueModal: boolean;
    isOpenOverlaySettingsModal: boolean;
    handleCloseContinueModal: (val: boolean) => void;
    handleCloseSettingsModal: (val: boolean) => void;
    handleToggleConnectedCalendar: (externalCalendarId: string, credentialId: number) => void;
    checkIsCalendarToggled: (externalId: string, credentialId: number) => boolean;
};
export {};
//# sourceMappingURL=useOverlayCalendar.d.ts.map