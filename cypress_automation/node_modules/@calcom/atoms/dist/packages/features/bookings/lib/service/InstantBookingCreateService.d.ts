import { CreateInstantBookingData, InstantBookingCreateResult } from '../dto/types';
import { IBookingCreateService } from '../interfaces/IBookingCreateService';
import { PrismaClient } from '../../../../prisma';
interface IInstantBookingCreateServiceDependencies {
    prismaClient: PrismaClient;
}
export declare function handler(bookingData: CreateInstantBookingData, deps: IInstantBookingCreateServiceDependencies): Promise<{
    message: "Success";
    meetingTokenId: number;
    bookingId: number;
    bookingUid: string;
    expires: Date;
    userId: number | null;
}>;
/**
 * Instant booking service that handles instant/immediate bookings
 */
export declare class InstantBookingCreateService implements IBookingCreateService {
    private readonly deps;
    constructor(deps: IInstantBookingCreateServiceDependencies);
    createBooking(input: {
        bookingData: CreateInstantBookingData;
    }): Promise<InstantBookingCreateResult>;
}
export {};
//# sourceMappingURL=InstantBookingCreateService.d.ts.map