import { RoutingFormSearchParams } from '../../../../../platform/types/index.ts';
import { BookerEvent, BookingCreateBody, RecurringBookingCreateBody } from '../../../types';
import { Tracking } from '../../handleNewBooking/types';
export type BookingOptions = {
    values: Record<string, unknown>;
    event: Pick<BookerEvent, "id" | "length" | "slug" | "schedulingType" | "recurringEvent">;
    date: string;
    duration: number | undefined | null;
    timeZone: string;
    language: string;
    rescheduleUid: string | undefined;
    rescheduledBy: string | undefined;
    username: string;
    metadata?: Record<string, string>;
    bookingUid?: string;
    seatReferenceUid?: string;
    hashedLink?: string | null;
    teamMemberEmail?: string | null;
    crmOwnerRecordType?: string | null;
    crmAppSlug?: string | null;
    crmRecordId?: string | null;
    pendingCrmTraceId?: string | null;
    orgSlug?: string;
    routingFormSearchParams?: RoutingFormSearchParams;
    isDryRunProp?: boolean;
    verificationCode?: string;
    rrHostSubsetIds?: number[];
};
export declare const mapBookingToMutationInput: ({ values, event, date, duration, timeZone, language, rescheduleUid, rescheduledBy, username, metadata, bookingUid, seatReferenceUid, hashedLink, teamMemberEmail, crmOwnerRecordType, crmAppSlug, crmRecordId, pendingCrmTraceId, orgSlug, routingFormSearchParams, isDryRunProp, verificationCode, rrHostSubsetIds, }: BookingOptions) => BookingCreateBody;
export declare const mapRecurringBookingToMutationInput: (booking: BookingOptions, recurringCount: number, tracking?: Tracking) => RecurringBookingCreateBody[];
//# sourceMappingURL=booking-to-mutation-input-mapper.d.ts.map