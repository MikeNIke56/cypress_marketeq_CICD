import { ReactNode } from 'react';
import { UseFormReturn } from 'react-hook-form';
import { z } from 'zod';
import { createEventTypeInput } from '../lib/types';
type CreateEventTypeFormValues = z.infer<typeof createEventTypeInput>;
export default function CreateEventTypeForm({ form, isManagedEventType, handleSubmit, pageSlug, isPending, urlPrefix, SubmitButton, isPlatform, }: {
    form: UseFormReturn<CreateEventTypeFormValues>;
    isManagedEventType: boolean;
    handleSubmit: (values: CreateEventTypeFormValues) => void;
    pageSlug?: string;
    isPending: boolean;
    urlPrefix?: string;
    SubmitButton: (isPending: boolean) => ReactNode;
    isPlatform?: boolean;
}): JSX.Element;
export {};
//# sourceMappingURL=CreateEventTypeForm.d.ts.map