import { PrismaClient } from '../../../../../prisma';
import { IBillingRepository } from '../billing/IBillingRepository';
import { ITeamBillingDataRepository } from './ITeamBillingDataRepository';
export declare class PrismaTeamBillingDataRepository implements ITeamBillingDataRepository {
    private prisma;
    private teamBillingRepo;
    private orgBillingRepo;
    constructor(prisma: PrismaClient, teamBillingRepo: IBillingRepository, orgBillingRepo: IBillingRepository);
    /** Fetch a single team with minimal data needed for billing */
    find(teamId: number): Promise<{
        name: string;
        id: number;
        metadata: import('@prisma/client/runtime/client').JsonValue;
        parentId: number | null;
        isOrganization: boolean;
    }>;
    /** Fetch a single team with minimal data needed for billing by looking up the TeamBilling or OrganizationBilling table */
    findBySubscriptionId(subscriptionId: string): Promise<{
        name: string;
        id: number;
        metadata: import('@prisma/client/runtime/client').JsonValue;
        parentId: number | null;
        isOrganization: boolean;
    } | null>;
    /** Fetch multiple teams with minimal data needed for billing */
    findMany(teamIds: number[]): Promise<{
        name: string;
        id: number;
        metadata: import('@prisma/client/runtime/client').JsonValue;
        parentId: number | null;
        isOrganization: boolean;
    }[]>;
}
//# sourceMappingURL=PrismaTeamBillingRepository.d.ts.map