import { PrismaClient } from '../../../../../prisma';
import { WebhookEventStatus } from '../../../../../prisma/enums';
export declare class PrismaWebhookEventRepository {
    private readonly prisma;
    constructor(prisma: PrismaClient);
    create(externalEventId: string): Promise<{
        id: string;
    } | null>;
    reacquireStale(externalEventId: string, staleThreshold: Date): Promise<{
        id: string;
    } | null>;
    updateStatus(id: string, status: WebhookEventStatus): Promise<void>;
}
//# sourceMappingURL=PrismaWebhookEventRepository.d.ts.map