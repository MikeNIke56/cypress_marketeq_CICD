import { Logger } from 'tslog';
import { IProrationEmailTasker } from './types';
export declare class ProrationEmailSyncTasker implements IProrationEmailTasker {
    private readonly logger;
    constructor(logger: Logger<unknown>);
    sendInvoiceEmail(payload: Parameters<IProrationEmailTasker["sendInvoiceEmail"]>[0]): Promise<{
        runId: string;
    }>;
    sendReminderEmail(payload: Parameters<IProrationEmailTasker["sendReminderEmail"]>[0]): Promise<{
        runId: string;
    }>;
    cancelReminder(payload: Parameters<IProrationEmailTasker["cancelReminder"]>[0]): Promise<{
        runId: string;
    }>;
}
//# sourceMappingURL=ProrationEmailSyncTasker.d.ts.map