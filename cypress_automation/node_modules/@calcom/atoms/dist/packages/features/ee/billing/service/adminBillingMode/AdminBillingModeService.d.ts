import { BillingMode } from '../../../../../prisma/enums';
import { AdminBillingRepository } from '../../repository/adminBilling/AdminBillingRepository';
import { IBillingProviderService } from '../billingProvider/IBillingProviderService';
export type UpdateBillingModeInput = {
    billingId: string;
    entityType: "team" | "organization";
    billingMode: BillingMode;
    minSeats: number | null;
    pricePerSeat: number | null;
};
export declare class AdminBillingModeService {
    private readonly adminBillingRepository;
    private readonly billingProviderService;
    constructor(adminBillingRepository: AdminBillingRepository, billingProviderService: IBillingProviderService);
    updateBillingMode(input: UpdateBillingModeInput): Promise<{
        success: true;
    }>;
    private updateStripePrice;
}
//# sourceMappingURL=AdminBillingModeService.d.ts.map