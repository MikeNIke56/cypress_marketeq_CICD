import { TaskWithSchema } from '@trigger.dev/sdk';
import { z } from 'zod';
declare const blastDunningEmailsSchema: z.ZodObject<{
    dryRun: z.ZodDefault<z.ZodBoolean>;
    excludedTeamIds: z.ZodDefault<z.ZodArray<z.ZodNumber, "many">>;
}, "strip", z.ZodTypeAny, {
    dryRun: boolean;
    excludedTeamIds: number[];
}, {
    dryRun?: boolean | undefined;
    excludedTeamIds?: number[] | undefined;
}>;
export declare const BLAST_DUNNING_EMAILS_JOB_ID = "billing.blast-dunning-emails";
/**
 * One-time task to send dunning emails to all teams currently in dunning.
 * Sends the email matching each team's current status without advancing state.
 * Use dryRun: true to preview counts before sending.
 *
 * Paginates DB queries and fans out email sends in batches to handle 10k+ records.
 */
export declare const blastDunningEmails: TaskWithSchema<typeof BLAST_DUNNING_EMAILS_JOB_ID, typeof blastDunningEmailsSchema>;
export {};
//# sourceMappingURL=blast-dunning-emails.d.ts.map