import { TraceContext } from '../../../../lib/tracing';
import { NextApiRequest, NextApiResponse } from 'next';
import { default as Stripe } from 'stripe';
export declare const config: {
    api: {
        bodyParser: boolean;
    };
};
export declare function handleStripePaymentSuccess(event: Stripe.Event, traceContext: TraceContext): Promise<void>;
export declare const handleSetupSuccess: (event: Stripe.Event, traceContext: TraceContext) => Promise<void>;
/**
 * @deprecated
 * We need to create a PaymentManager in `@calcom/lib`
 * to prevent circular dependencies on App Store migration
 */
export default function handler(req: NextApiRequest, res: NextApiResponse): Promise<void>;
//# sourceMappingURL=webhook.d.ts.map