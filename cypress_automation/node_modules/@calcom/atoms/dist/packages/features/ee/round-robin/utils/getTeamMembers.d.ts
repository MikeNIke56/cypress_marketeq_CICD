import { getEventTypeResponse } from '../../../bookings/lib/handleNewBooking/getEventTypesFromDB';
import { IsFixedAwareUser } from '../../../bookings/lib/handleNewBooking/types';
import { BookingSelectResult } from './bookingSelect';
type Attendee = {
    name: string;
    id: number;
    email: string;
    timeZone: string;
    locale: string | null;
    bookingId: number | null;
    phoneNumber: string | null;
    noShow: boolean | null;
};
type OrganizerType = getEventTypeResponse["hosts"][number]["user"] | IsFixedAwareUser | {
    id: number;
    email: string;
    name: string | null;
    locale: string | null;
    timeZone: string;
    username: string | null;
};
export declare function getTeamMembers({ eventTypeHosts, attendees, organizer, previousHost, reassignedHost, }: {
    eventTypeHosts: getEventTypeResponse["hosts"];
    attendees: Attendee[];
    organizer: OrganizerType;
    previousHost: BookingSelectResult["user"] | getEventTypeResponse["hosts"][number]["user"] | null;
    reassignedHost: getEventTypeResponse["hosts"][number]["user"];
}): Promise<{
    id: number;
    email: string;
    name: string;
    timeZone: string;
    language: {
        translate: import('i18next').TFunction<string, undefined>;
        locale: string;
    };
}[]>;
export {};
//# sourceMappingURL=getTeamMembers.d.ts.map