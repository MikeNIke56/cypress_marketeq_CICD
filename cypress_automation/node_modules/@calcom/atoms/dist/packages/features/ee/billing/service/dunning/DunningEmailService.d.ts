import { PrismaClient } from '../../../../../prisma';
import { DunningServiceFactory } from './DunningServiceFactory';
export interface IDunningEmailServiceDeps {
    dunningServiceFactory: DunningServiceFactory;
    prismaClient: PrismaClient;
}
export declare class DunningEmailService {
    private deps;
    constructor(deps: IDunningEmailServiceDeps);
    private findDunningRecord;
    sendWarningEmail(teamId: number): Promise<void>;
    sendSoftBlockEmail(teamId: number): Promise<void>;
    sendPauseEmail(teamId: number): Promise<void>;
    sendCancellationEmail(teamId: number): Promise<void>;
}
//# sourceMappingURL=DunningEmailService.d.ts.map