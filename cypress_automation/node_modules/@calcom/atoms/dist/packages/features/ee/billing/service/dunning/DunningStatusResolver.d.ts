import { TeamRepository } from '../../../teams/repositories/TeamRepository';
import { IBillingRepository } from '../../repository/billing/IBillingRepository';
import { DunningStatus } from './DunningState';
import { IDunningService } from './IDunningService';
export interface IDunningStatusResolverDeps {
    teamDunningService: IDunningService;
    orgDunningService: IDunningService;
    teamRepository: TeamRepository;
    teamBillingRepository: IBillingRepository;
    orgBillingRepository: IBillingRepository;
}
export declare class DunningStatusResolver {
    private deps;
    constructor(deps: IDunningStatusResolverDeps);
    getEffectiveStatus(teamId: number): Promise<DunningStatus>;
}
//# sourceMappingURL=DunningStatusResolver.d.ts.map