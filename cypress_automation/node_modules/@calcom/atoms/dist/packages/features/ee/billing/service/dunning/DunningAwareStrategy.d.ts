import { ISeatBillingStrategy, SeatChangeContext, StripeInvoiceData, BaseSeatBillingStrategy } from '../seatBillingStrategy/ISeatBillingStrategy';
import { IDunningService } from './IDunningService';
export interface IDunningAwareStrategyDeps {
    inner: ISeatBillingStrategy;
    dunningService: IDunningService;
    billingId: string;
    teamId: number;
    subscriptionId: string;
}
export declare class DunningAwareStrategy extends BaseSeatBillingStrategy {
    private readonly deps;
    readonly strategyName: string;
    constructor(deps: IDunningAwareStrategyDeps);
    onSeatChange(context: SeatChangeContext): Promise<void>;
    onInvoiceUpcoming(subscriptionId: string): Promise<{
        applied: boolean;
    }>;
    onRenewalPaid(subscriptionId: string, periodStart: Date): Promise<{
        reset: boolean;
    }>;
    onPaymentFailed(invoice: StripeInvoiceData, reason: string): Promise<{
        handled: boolean;
    }>;
    onPaymentSucceeded(invoice: StripeInvoiceData): Promise<{
        handled: boolean;
    }>;
}
//# sourceMappingURL=DunningAwareStrategy.d.ts.map