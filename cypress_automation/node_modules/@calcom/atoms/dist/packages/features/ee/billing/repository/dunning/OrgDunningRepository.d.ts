import { PrismaClient } from '../../../../../prisma';
import { DunningStatus } from '../../../../../kysely/types.ts';
import { IDunningRepository, RawDunningRecord, RawDunningRecordForBanner, RawDunningRecordForBilling, UpsertData } from './IDunningRepository';
export declare class OrgDunningRepository implements IDunningRepository {
    private prisma;
    constructor(prisma?: PrismaClient);
    findByBillingId(billingId: string): Promise<RawDunningRecord | null>;
    upsert(billingId: string, data: UpsertData): Promise<RawDunningRecord>;
    findEntitiesToAdvance(): Promise<RawDunningRecord[]>;
    findByBillingIds(billingIds: string[]): Promise<RawDunningRecordForBilling[]>;
    findByTeamIds(teamIds: number[]): Promise<RawDunningRecordForBanner[]>;
    advanceStatus(billingId: string, newStatus: DunningStatus): Promise<RawDunningRecord>;
}
//# sourceMappingURL=OrgDunningRepository.d.ts.map