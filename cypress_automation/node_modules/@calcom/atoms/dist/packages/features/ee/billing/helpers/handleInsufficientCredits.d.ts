import { PrismaClient } from '../../../../prisma';
import { CreditUsageType } from '../../../../prisma/enums';
export declare function handleInsufficientCredits({ userId, teamId, creditFor, prismaClient, context, }: {
    userId?: number | null;
    teamId?: number | null;
    creditFor: CreditUsageType;
    prismaClient: PrismaClient;
    context?: Record<string, unknown>;
}): Promise<void>;
//# sourceMappingURL=handleInsufficientCredits.d.ts.map