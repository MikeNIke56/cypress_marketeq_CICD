import { PrismaWebhookEventRepository } from '../../repository/webhookEvent/PrismaWebhookEventRepository';
export interface WebhookEventServiceDeps {
    webhookEventRepository: PrismaWebhookEventRepository;
}
export declare class WebhookEventService {
    private deps;
    constructor(deps: WebhookEventServiceDeps);
    tryAcquire(externalEventId: string): Promise<{
        id: string;
    } | null>;
    markCompleted(id: string): Promise<void>;
    markFailed(id: string): Promise<void>;
}
//# sourceMappingURL=WebhookEventService.d.ts.map