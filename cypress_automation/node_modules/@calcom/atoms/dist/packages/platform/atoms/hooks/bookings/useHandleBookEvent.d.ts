import { UseBookingFormReturnType } from '../../../../features/bookings/Booker/hooks/useBookingForm';
import { BookingCreateBody } from '../../../../features/bookings/lib/bookingCreateBodySchema';
import { BookerEvent } from '../../../../features/bookings/types';
import { RoutingFormSearchParams } from '../../../types/index.ts';
import { UseCreateBookingInput } from './useCreateBooking';
type Callbacks = {
    onSuccess?: () => void;
    onError?: (err: unknown) => void;
};
type UseHandleBookingProps = {
    bookingForm: UseBookingFormReturnType["bookingForm"];
    event?: {
        data?: Pick<BookerEvent, "id" | "isDynamic" | "metadata" | "recurringEvent" | "length" | "slug" | "schedulingType"> | null;
    };
    metadata: Record<string, string>;
    hashedLink?: string | null;
    handleBooking: (input: UseCreateBookingInput, callbacks?: Callbacks) => void;
    handleInstantBooking: (input: BookingCreateBody, callbacks?: Callbacks) => void;
    handleRecBooking: (input: BookingCreateBody[], callbacks?: Callbacks) => void;
    locationUrl?: string;
    routingFormSearchParams?: RoutingFormSearchParams;
    isBookingDryRun?: boolean;
    rrHostSubsetIds?: number[];
};
export declare const useHandleBookEvent: ({ bookingForm, event, metadata, hashedLink, handleBooking, handleInstantBooking, handleRecBooking, locationUrl, routingFormSearchParams, isBookingDryRun, rrHostSubsetIds, }: UseHandleBookingProps) => (inputTimeSlot?: string) => void;
export {};
//# sourceMappingURL=useHandleBookEvent.d.ts.map