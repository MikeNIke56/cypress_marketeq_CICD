import { CALENDARS, ERROR_STATUS, SUCCESS_STATUS } from '../../../constants/index.ts';
import { ApiErrorResponse } from '../../../types/index.ts';
export interface UseCheckProps {
    onCheckError?: OnCheckErrorType;
    calendar: (typeof CALENDARS)[number];
    initialData?: {
        status: typeof SUCCESS_STATUS | typeof ERROR_STATUS;
        data: {
            allowConnect: boolean;
            checked: boolean;
        };
    };
}
export type OnCheckErrorType = (err: ApiErrorResponse) => void;
export declare const getQueryKey: (calendar: (typeof CALENDARS)[number]) => string[];
export declare const useCheck: ({ onCheckError, calendar, initialData }: UseCheckProps) => {
    allowConnect: boolean;
    checked: boolean;
    refetch: () => void;
};
//# sourceMappingURL=useCheck.d.ts.map