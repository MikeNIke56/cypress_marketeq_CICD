import { ReactNode } from 'react';
import { API_VERSIONS_ENUM } from '../../constants/index.ts';
import { i18nProps } from './languages';
export type BaseCalProviderProps = {
    children?: ReactNode;
    clientId: string;
    accessToken?: string;
    options: {
        refreshUrl?: string;
        apiUrl: string;
        readingDirection?: "ltr" | "rtl";
    };
    autoUpdateTimezone?: boolean;
    onTimezoneChange?: () => void;
    onTokenRefreshStart?: () => void;
    onTokenRefreshSuccess?: () => void;
    onTokenRefreshError?: (error: string) => void;
    version?: API_VERSIONS_ENUM;
    organizationId?: number;
    isEmbed?: boolean;
    isOAuth2?: boolean;
} & i18nProps;
export declare function BaseCalProvider({ clientId, accessToken, options, children, labels, autoUpdateTimezone, language, organizationId, onTimezoneChange, onTokenRefreshStart, onTokenRefreshSuccess, onTokenRefreshError, isEmbed, isOAuth2 }: BaseCalProviderProps): JSX.Element;
//# sourceMappingURL=BaseCalProvider.d.ts.map