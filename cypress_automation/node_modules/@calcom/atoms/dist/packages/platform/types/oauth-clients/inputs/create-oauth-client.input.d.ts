import { PERMISSION_MAP } from '../../../constants/index.ts';
export declare const ARE_DEFAULT_EVENT_TYPES_ENABLED_DOCS = "If true, when creating a managed user the managed user will have 4 default event types: 30 and 60 minutes without Cal video, 30 and 60 minutes with Cal video. Set this as false if you want to create a managed user and then manually create event types for the user.";
export declare const ARE_CALENDAR_EVENTS_ENABLED_DOCS = "If true and if managed user has calendar connected, calendar events will be created. Disable it if you manually create calendar events. Default to true.";
export declare class CreateOAuthClientInput {
    logo?: string;
    name: string;
    redirectUris: string[];
    permissions: Array<keyof typeof PERMISSION_MAP | "*">;
    bookingRedirectUri?: string;
    bookingCancelRedirectUri?: string;
    bookingRescheduleRedirectUri?: string;
    areEmailsEnabled?: boolean;
    areDefaultEventTypesEnabled?: boolean | undefined;
    areCalendarEventsEnabled?: boolean | undefined;
}
//# sourceMappingURL=create-oauth-client.input.d.ts.map